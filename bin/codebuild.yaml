version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.x
    commands:
      - echo Installing dependencies...
      - go mod tidy

  build:
    commands:
      - echo Building the project...
      - go build -o main .

  post_build:
    commands:
      - echo Build complete.
